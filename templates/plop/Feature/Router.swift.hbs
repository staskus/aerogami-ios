import UIKit

class {{properCase name}}Router {
    weak var viewController: {{properCase name}}ViewController?

    init() {
    }

    func route(to route: {{properCase name}}.Route) {
        switch route {
        default: break
        }
    }
}
