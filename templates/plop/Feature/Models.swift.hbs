import Foundation
import TravelKit

struct {{properCase name}} {
    struct Data: Equatable {
    }

    struct ViewModel: FeatureViewModel {
        let state: ViewState<{{properCase name}}.ViewModel.Content>
        let title: String

        struct Content: FeatureContentViewModel {
        }
    }

    enum Action {
        case load
    }

    enum Route {
    }
}
