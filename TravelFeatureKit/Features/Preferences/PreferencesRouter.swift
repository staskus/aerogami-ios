import UIKit

class PreferencesRouter {
    weak var viewController: PreferencesViewController?

    init() {
    }

    func route(to route: Preferences.Route) {
        switch route {
        default: break
        }
    }
}
