import UIKit

class FeedRouter {
    weak var viewController: FeedViewController?

    init() {
    }

    func route(to route: Feed.Route) {
        switch route {
        default: break
        }
    }
}
